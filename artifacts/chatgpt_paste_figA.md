FIG-A — figure code + data review (round 1 of figure audit)

I am auditing four matplotlib figures (generated by src/pluvial_flood_risk/figures.py) against their data. I cannot upload images to you in this session, so I am giving you (1) the verbatim plotting code, (2) the exact data values, and (3) my visual findings. Please review the CODE and the DESIGN and answer the numbered questions. Do not re-derive the science; focus on figure correctness, legibility, and publication quality.

== MAPPING ==
Fig1 workflow_schematic.png <- plot_workflow_schematic() [conceptual, no data]
Fig2 spatial_cv_folds.png <- plot_spatial_cv_bars() [data: spatial_cv_folds.csv]
Fig3 jaccard_by_resolution.png <- plot_jaccard_ladder() [data: jaccard_by_resolution.csv]
Fig4 adaptive_ablation.png <- plot_adaptive_ablation() [data: adaptive_vs_fixed_ablation.csv]

== DATA (locked values) ==
Fig2 folds: acc [0.7551, 0.7600, 0.7727, 0.7143, 0.9167], f1 [0.850, 0.850, 0.872, 0.812, 0.944]
Fig3: fine=R10; coarse in {8,9}. R8: mean J=0.1667/F1=0.286; max J=1.0/F1=1.0; p90 J=1.0/F1=1.0. R9: mean J=0.9767/F1=0.988; max J=1.0/F1=1.0; p90 J=0.9767/F1=0.988.
Fig4: fixed coarse=141, adaptive mixed=3933, uniform fine=6909.

== CODE (verbatim, key parts) ==

plot_adaptive_ablation (Fig4):
  labels = ["Fixed coarse", "Adaptive mixed", "Uniform fine"]
  values = [141, 3933, 6909]
  ax.bar(labels, values, color=[...])
  ax.set_ylabel("Cell count"); value labels on top; ylim(0, 6909*1.12); grid y only

plot_jaccard_ladder (Fig3):
  1x2 subplots sharex; x = coarse_res (values 8,9); three series mean/max/p90
  linestyles mean=-, max=--, p90=: ; ylim 0..1.05; legend title "Rollup"
  left panel title "Fine = R10", right panel title ""

plot_spatial_cv_bars (Fig2):
  5 folds grouped bars accuracy + f1; mean +- SD drawn as axhline + axhspan per metric
  ylim 0..1.05; legend Accuracy/F1

plot_workflow_schematic (Fig1):
  4 columns (Open inputs / H3 assembly / Learning / Diagnostics); colored boxes;
  Sandy negative-control dashed box at bottom + dashed arrow into col 4

== MY VISUAL FINDINGS (7) ==
1. Fig4: linear axis -> "Fixed coarse" (141) is a ~2% sliver, nearly invisible; fails to show 27.9x vs 0.57 ratio.
2. Fig3: only 2 coarse resolutions; "max" is flat 1.0 both panels; p90 coincides with max at R8 and with mean at R9 -> legend has 3 entries but curves overlap.
3. Fig3: fine R10 never on the axis; "Fine = R10" title only on left panel.
4. Fig2: mean+-SD bands for accuracy and F1 overlap; 2 legend entries but 4 visual channels (bars + dashed lines + shaded bands).
5. Fig2: Fold4 outlier (0.917, n=24) not flagged in-figure.
6. Fig1: stage-4 PFI_h box ~4 wrapped lines at 7.4pt; stage-3 has 4 items (others 3); Sandy arrow has no clear target box.
7. Fig1: "constant-class baselines" + "Baselines: logistic / ponding rule" redundant wording.

== QUESTIONS ==
Q1. For Fig4, is a log-scale y-axis the right fix, or would a two-line annotation (27.9x vs 0.57 ratio) or a second "ratio" panel be better for a journal figure? Give the recommended approach.
Q2. For Fig3, given max/p90 are structurally ~1.0 by construction, should I (a) keep all three series and annotate, (b) plot only "mean" with a note, or (c) something else? Also confirm whether the "Fine = R10" title belongs on both panels or neither.
Q3. For Fig2, how to declutter the mean+-SD bands while keeping the information (paired points? error bars? single reference line)?
Q4. For Fig1, concrete fixes for text overflow and the Sandy arrow target.
Q5. Any other code-level bugs or publication issues you can see in the functions above?
